## Packages
framer-motion | Smooth animations for page transitions and UI elements
recharts | Analytics charts for the dashboard
date-fns | Date formatting for responses and sets
clsx | Conditional class merging
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- Bilingual support (English/Bangla) via client-side context
- Replit Auth is already integrated on the backend, use /api/auth/user
- Background pattern should be a subtle Islamic geometric motif
- API routes follow @shared/routes structure strictly
